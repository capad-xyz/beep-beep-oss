# Secrets / environment
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Matrix / bridge runtime data & generated config (never commit — contains secrets)
data/
infra/data/
infra/caddy/Caddyfile          # generated from Caddyfile.example
infra/**/homeserver.yaml
infra/**/registration.yaml
*.db
*.db-shm
*.db-wal
*.log
*.signing.key
homeserver.signing.key
*.registration.yaml
config.yaml
!config.example.yaml
!*.example.yaml

# Rust
/target/
**/target/
Cargo.lock.bak
*.pdb

# Node / frontend
node_modules/
dist/
dist-ssr/
.vite/
*.local

# Tauri
src-tauri/target/
src-tauri/gen/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.swp

# Local nuke/reset backups — contain signing keys + tokens, never commit
.nuke-backup-*/

# Personal Claude Code settings (permission rules etc.) — never commit to the OSS repo
/.claude/settings.local.json
/.claude/worktrees/
