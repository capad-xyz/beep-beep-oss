{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window permissions. Without this file the webview has NO ACL grants at all — in Tauri v2 that silently rejects every `listen()` call, which kills all live backend→UI events (room-list, timeline-items, sync-state, typing) while invoke-based request/response keeps working. core:default includes core:event:default (listen/unlisten/emit).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
